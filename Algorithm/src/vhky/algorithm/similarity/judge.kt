package vhky.algorithm.similarity

import vhky.algorithm.data.ImageData

/**
 * Find clusters formed by similar images in given image list
 *
 * @param data list of images
 * @return list of the found clusters
 */

fun cluster(data : List<ImageData>) : List<List<ImageData>> = emptyList()


/**
 * Judge if two images are similar
 *
 * @param image1 image 1
 * @param image2 image 2
 * @return whether they are similar
 */
fun judgeTwoImage(image1 : ImageData, image2 : ImageData) : Boolean = false